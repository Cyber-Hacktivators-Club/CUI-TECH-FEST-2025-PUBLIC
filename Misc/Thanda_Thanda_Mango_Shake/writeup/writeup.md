Pawde loves a thanda thanda mango shake

so you'll use stegolsb to get the txt file, then to that its the hex of an image, use that to get another image
steghide passwd in metadata, extract the text from the image, decode it and get the flag